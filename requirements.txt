# Core dependencies
pandas==2.0.3  # Using an older version compatible with NumPy 1.x
numpy==1.26.4  # Last 1.x version before NumPy 2.0
matplotlib==3.8.2  # Compatible with numpy 1.x
seaborn==0.13.0  # Compatible with numpy 1.x
scikit-learn==1.4.0  # Compatible with numpy 1.x
scipy==1.11.4  # Compatible with numpy 1.x

# New dependencies for modern architecture
pydantic>=2.0.0  # For configuration management
python-dotenv>=1.0.0  # For environment variable loading
click>=8.1.0  # For CLI
rich>=13.0.0  # For rich console output
tqdm>=4.65.0  # For progress bars

# Existing dependencies
jupyter==1.1.1
statsmodels==0.14.1  # Compatible with numpy 1.x
openpyxl==3.0.10  # For Excel file support
plotly==5.14.1  # For interactive visualizations
nbformat>=5.1.0  # Required for newer notebooks
shap>=0.44.0  # For model explainability, compatible with NumPy 2.0
optuna==3.5.0  # For hyperparameter optimization
joblib==1.3.2  # For parallel processing in Optuna

# Development dependencies (optional, install with pip install -r requirements-dev.txt)
# pytest>=7.3.0
# pytest-cov>=4.1.0
# black>=23.3.0
# isort>=5.12.0
# mypy>=1.3.0